apiVersion: v1
kind: PersistentVolume
metadata:
  name: solr-pv
  labels:
    app: solr
    node: pv
spec:
  storageClassName: manual
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/var/solr/data"
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: zk-pv
  labels:
    app: zoo
    node: pv
spec:
  storageClassName: manual
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/var/zk/data"
